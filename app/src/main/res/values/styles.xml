<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/material_color_grey_primary</item>
        <item name="android:colorPrimaryDark">@color/material_color_grey_primary_dark</item>
        <item name="android:colorAccent">@color/material_color_orange_accent</item>
    </style>

</resources>
